export enum IncomeType {
  Wages = 'Wages',
  Benefits = 'Benefits',
  Other = 'Other',
}

export enum PropertyType {
  DomesticProperty = 'DomesticProperty',
  Vehicle = 'Vehicle',
}

export enum DebtType {
  OwnDomicile = 'OwnDomicile',
  Other = 'Other',
}

export enum Currency {
  ISK = 'ISK',
}